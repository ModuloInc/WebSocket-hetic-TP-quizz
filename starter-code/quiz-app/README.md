# Quiz App

## Demarrer avec Docker

Depuis le dossier `quiz-app/` :

```bash
docker compose up -d --build
```

Pour arreter :

```bash
docker compose down
```

## URL d'acces

- Host: `http://localhost:5173/host/`
- Player: `http://localhost:5174/player/`
